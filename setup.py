from setuptools import setup

setup(
  name='lineart',
  version='0.1',
  author='Harrison Trypp Cook',
  setup_requires=[],
  install_requires=[],
  packages=[
    'lineart',
    #'lineart.artists',
    #'lineart.brushes',
  ]
)
